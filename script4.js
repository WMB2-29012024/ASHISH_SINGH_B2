const num = 38;

const addDigits =  (inputNum) => {
    let stringNum = String(inputNum);

    if (stringNum.length === 1) {
        return inputNum;
    }

    while (stringNum.length !== 1) {
        let sum = 0;
        for (let i = 0; i<stringNum[i]); {
    }
    stringNum = String(sum);
}
return Number(stringNum);
};

console.log(addDigits(num));
